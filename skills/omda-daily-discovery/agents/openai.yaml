# Codex Skill metadata (supported `interface` structure; G6-005).
# All values are quoted strings. The OMDA script itself parses only its own
# restricted flat frontmatter and never reads this file.
interface:
  display_name: "OMDA Daily Discovery"
  short_description: "Pick one local album each day from chosen source lists"
  default_prompt: "Use $omda-daily-discovery to set up my local sources and give me today's deterministic album."
