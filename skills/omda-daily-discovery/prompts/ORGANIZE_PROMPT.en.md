Please organize the music material I provide into one OMDA Source Markdown
file.

Rules (must follow):

1. Organize **one** curator/source at a time and produce **one** source file;
   never merge several people's material into a single list.
2. Use only information that actually appears in the material I provide;
   leave unknown fields blank — never invent or guess (unless I explicitly
   ask and provide the source).
3. Fill in the restricted OMDA flat frontmatter first (top-level
   `key: value` lines; no indentation, no nesting): `source_id` (the unique
   short slug I specify: lowercase letters/digits/hyphens), `display_name`,
   `curator`, `provenance`, `sharing_note` as I dictate; `rating_scale`
   only if that source actually has ratings. For a new OMDA-native list,
   use `10-point-integer`: leave Rating blank or enter a whole number from
   1 to 10, with no decimals.
4. Album table: copy the template header row exactly, one album per row.
   Artist and Album are required; fill Year, Genre, Rating, Note when
   available, leave blank when unsure.
5. Do **not** write any personal heard/skip state — that belongs to MY own
   profile file (`profile/MY_PROFILE.md`), not to a source list.
6. Keep Rating exactly as expressed in the source material; do not convert
   or score it on your own. If the material uses another scale, record that
   scale truthfully in `rating_scale`. Convert to ten-point integers only
   when the Owner explicitly asks. Note may quote one short remark with its
   attribution.
7. Give the result back to me for confirmation; do not submit it to any
   tool or upload it without my confirmation.

My material:
(paste one friend's / critic's / community's / your own playlists, reviews,
listening logs, etc.)
