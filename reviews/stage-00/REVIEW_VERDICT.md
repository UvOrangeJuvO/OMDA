# Gate G0 Independent Review Verdict

## Reviewed object

- Reviewer: GPT-5.6 Sol in Codex
- Review date: 2026-08-19
- Gate: G0 — implementation plan and architecture baseline
- Base SHA: `8a2e82072afca8fe7217c9fc79288d99a94e01ce`
- Candidate SHA: `488860b497f3a179a12ce38082d337b48dda6e2f`
- Candidate branch observed: `exec/g0-plan`
- Executor report: `reviews/stage-00/EXECUTOR_REPORT.md`
- Review scope: full `base..candidate` diff, authoritative repository specifications, governance state, risk register, and current worktree disclosure

The candidate contains only the four declared governance changes and no production implementation. The base is the merge-base and direct parent of the candidate. `git diff --check` passed for the reviewed range.

## Findings

### [P1] G0-001 — Port contracts are scheduled after the Orchestrator that requires them

- Location: `governance/IMPLEMENTATION_PLAN.md`, B.3/B.9, T2.6 and T3.1 (approximately lines 105–142, 267–297)
- Evidence: the architecture states that the Orchestrator invokes adapters through Ports, but the plan does not define the six Port interfaces and domain error taxonomy until G3/T3.1. G2/T2.6 already intends to implement the full run state machine, including generation, delivery, journal/history commit and recovery. That implementation cannot be built and contract-tested cleanly before the required application boundaries exist.
- Impact: G2 would either depend directly on concrete G1 SQLite/storage code, invent private ad-hoc interfaces and later refactor them in G3, or implement an untestable skeleton. All three outcomes undermine the declared inward dependency direction and make the highest-risk Gate unstable.
- Violated contract: `OMDA_AGENT_HANDOFF_SPEC.md` §3.1–3.2 and §10; Master Plan §4; the candidate's own B.2/B.3/B.9.
- Required correction: move the **minimal application Port protocols and domain error taxonomy needed by G2** to G1 or the beginning of G2, before any Orchestrator work. G2/T2.6 must run entirely against fakes/in-memory test implementations of those contracts. G3 should implement concrete external data/browser adapters and may refine source-specific contracts only through normal Gate review; it must not be the first point at which the Orchestrator's essential boundaries exist.
- Acceptance test for closure: the revised task dependency graph has no G2 task depending on a contract first delivered in G3, and explicitly states how G2 exercises `GENERATE`, `DELIVER`, journal and history transitions with fakes while concrete LLM/PushPlus/RYM adapters remain later work.

### [P1] G0-002 — The pure Recommendation Core is described as reading runtime history/storage

- Location: `governance/IMPLEMENTATION_PLAN.md`, T2.4 dependency and T2.6 rollback (approximately lines 247–278)
- Evidence: T2.4 lists `T1.4（history 读取）` as a dependency for Album filtering even though B.1 promises a pure Core with no database driver or persistence implementation. T2.6 then says runtime data under `var/` can be reset as rollback.
- Impact: the first statement permits persistence concerns to leak into Album selection; the second is unsafe once `var/` contains official local history or recovery evidence. Deleting/resetting runtime data is not a valid rollback for user state.
- Violated contract: `OMDA_AGENT_HANDOFF_SPEC.md` §3.1, §3.3, §4 and §9; Master Plan §4 Storage and §6; `AGENTS.md` non-negotiable history and destructive-action rules.
- Required correction: state that the Application Orchestrator/History Port reads a versioned history snapshot or exclusion set and supplies it as an immutable domain input to Core. Core must not read SQLite, `HistoryStore`, or files. Replace the `var/` reset rollback language with a distinction between disposable test databases and protected real runtime data; real state requires backup/migration/recovery or a non-destructive compensating procedure.
- Acceptance test for closure: the plan requires an architecture test proving no persistence imports/calls from Core, includes a unit test that supplies historical exclusions as input, and contains no instruction that treats real runtime history as safely deletable.

### [P2] G0-003 — Current handoff worktree is not clean and WorkBuddy memory has no repository policy

- Location: current worktree outside the reviewed candidate; `.workbuddy/memory/2026-08-19.md`
- Evidence: during independent review, `git status --short` returned `?? .workbuddy/`. The Executor's final handoff stated that the post-commit worktree was clean. The file repeats candidate metadata and was likely generated by WorkBuddy after or around handoff, but its exact creation timing cannot be established from the commit.
- Impact: future reviews can miss untracked executor state, and a later broad staging action could commit tool memory accidentally. The reviewed candidate itself is unaffected.
- Required correction: establish an explicit policy. For the current local workflow, add `.workbuddy/` to `.gitignore` unless the Owner deliberately chooses to version selected WorkBuddy configuration. Update the repair report to disclose the observed untracked file and show a clean status after applying the policy. Do not delete the file merely to hide the discrepancy.
- Acceptance test for closure: `git check-ignore -v .workbuddy/memory/2026-08-19.md` identifies the intended rule and `git status --short` is empty after the repair commit.

### [P2] G0-004 — Risk references and priorities are internally inconsistent

- Location: `governance/IMPLEMENTATION_PLAN.md`, E and G/OD-4 (approximately lines 478–522); `governance/RISK_REGISTER.md`
- Evidence: R-010 is P1 in the authoritative risk table but is summarized as P2 in the plan. OD-4 cites R-012 for MusicBrainz license/limiting concerns, while R-012 is the timezone/daily-boundary risk; the relevant source risks are R-006 and, for network behavior, R-005 or a new explicit limit risk.
- Impact: Gate owners and reviewers may apply the wrong blocking rule or look at the wrong mitigation evidence.
- Required correction: make the priority and references consistent in all governance files.
- Acceptance test for closure: every risk ID mentioned in the plan resolves to the same priority and subject in `RISK_REGISTER.md`.

### [P2] G0-005 — The plan turns ordinary implementation defaults into ten Owner/ADR blockers

- Location: `governance/IMPLEMENTATION_PLAN.md`, G/OD-1 through OD-10 and the rule following the table
- Evidence: the plan says every open item must be confirmed by the Owner or settled through ADR before implementation. Several entries are ordinary reversible engineering choices within an accepted Gate, such as the Python baseline, environment-variable secret injection, local Markdown as the default dry-run output, and the precise non-flaky test technique.
- Impact: this needlessly burdens the Owner, encourages ADR proliferation and conflicts with the stated goal of a simple, low-friction Executor workflow. It also makes the plan operationally blocked despite claiming readiness.
- Required correction: classify the list into: (a) Owner product decisions, (b) architecture/contract decisions requiring ADR when they change an accepted contract, and (c) reversible implementation choices the Executor may propose and the Reviewer may evaluate at the relevant Gate. Retain Owner control over licenses, data scope and meaningful product semantics; do not require an ADR for every technical default.
- Acceptance test for closure: each item has an owner/decision mechanism and latest decision Gate, and ordinary reversible choices no longer automatically require Owner intervention or ADR.

## Acceptance matrix

| G0 criterion | Status | Evidence |
|---|---|---|
| Correct product understanding and ten invariants | PASS | Plan A.1–A.3 and Executor matrix |
| Minimal Core/Adapter architecture | FAIL | G0-001 and G0-002 leave sequencing/coupling contradictions |
| G1–G5 tasks with tests, acceptance, dependencies and rollback | PARTIAL | Structure is comprehensive; dependency and rollback corrections required |
| Test strategy covers the normative minimum | PASS WITH NOTE | D.1–D.9 cover required classes; exact equal-opportunity method may be selected as a reviewed implementation choice |
| Risk register is complete and consistent | FAIL | G0-004 |
| Explicit non-goals prevent scope creep | PASS | Plan F |
| Open decisions are actionable without governance overload | FAIL | G0-005 |
| G0 contains no production code | PASS | Exact diff contains governance files only |
| Exact-SHA handoff and clean status | PARTIAL | SHA is exact; candidate itself is coherent; current `.workbuddy/` state is undisclosed/untracked |

## Checks performed

- Verified both full SHAs resolve to commits.
- Verified base is the merge-base/direct parent of candidate.
- Inspected `git log`, complete diff name/status/stat and `git diff --check`.
- Read `AGENTS.md`, both normative specifications, the operations handbook, the full proposed plan, risk register, project state and Executor report.
- Parsed/inspected the current project state and compared references across governance artifacts.
- Inspected the current untracked `.workbuddy` file without treating its contents as authority.
- No production tests exist or were expected at G0; no live network or external-service checks were performed.

## Required repair scope

This is a small G0 document repair, not a redesign. The Executor should modify only:

- `governance/IMPLEMENTATION_PLAN.md`
- `governance/RISK_REGISTER.md` if needed for corrected references/classification
- `reviews/stage-00/REPAIR_REPORT.md`
- `governance/PROJECT_STATE.json` as required by the repair protocol
- `.gitignore` solely for the explicit `.workbuddy/` policy

It must not write production code or enter G1. It should create a new repair commit on `exec/g0-plan`, report the new candidate SHA, and request re-review against the original base.

## Verdict

**CHANGES_REQUESTED**

Open blocking findings: G0-001 (P1), G0-002 (P1). G0 is not accepted, must not be merged into `main`, and G1 must not begin.

---

## Re-review 1 — candidate `7bec77f3055c9afdad0f61593e985bb1eda8acfc`

### Re-review identity and method

- Original base: `8a2e82072afca8fe7217c9fc79288d99a94e01ce`
- Original candidate: `488860b497f3a179a12ce38082d337b48dda6e2f`
- Reviewer verdict commit: `13c57f433a3fdff626c3497d9ce5784f8490f358`
- Repair candidate: `7bec77f3055c9afdad0f61593e985bb1eda8acfc`
- Repair report: `reviews/stage-00/REPAIR_REPORT.md`

The Reviewer inspected the full `13c57f4..7bec77f` repair delta and regressed the original `8a2e820..7bec77f` range. The repair commit contains only `.gitignore` and governance artifacts, has no whitespace errors, resolves from the original base through the prior candidate and verdict, and leaves `git status --short` empty. `git check-ignore -v .workbuddy/memory/2026-08-19.md` resolves to `.gitignore:4:.workbuddy/`.

### Original finding status

| Finding | Re-review status | Evidence |
|---|---|---|
| G0-001 (P1) Port contracts after Orchestrator | **CLOSED for the original defect** | T1.6 now defines minimum Ports/fakes before G2; T2.6 explicitly tests against fakes |
| G0-002 (P1) Core/storage coupling and unsafe rollback | **OPEN** | Core input boundary is corrected, but T1.4 still says ``Rollback: `var/` 可删可重建`` at approximately line 199 |
| G0-003 (P2) WorkBuddy memory policy | **CLOSED** | `.workbuddy/` ignored; worktree clean; directory not deleted to hide it |
| G0-004 (P2) Risk inconsistency | **CLOSED** | R-010 is consistently P1; OD-4 now points to R-006/R-005 |
| G0-005 (P2) Owner/ADR overload | **CLOSED** | Open decisions now distinguish Owner, contract/ADR and reversible implementation choices |

### [P1] G0-002 remains open — T1.4 still authorizes deletion of real runtime history

- Location: `governance/IMPLEMENTATION_PLAN.md`, T1.4 rollback (approximately line 199)
- Evidence: the revised B.5 and T2.6 correctly protect official history and recovery evidence, but T1.4 still states ``Rollback：`var/` 可删可重建（非真源）``. `var/` is explicitly planned to contain official pick history, Album history, delivery receipts and the run journal, so “not community source of truth” does not make it disposable.
- Repair-report discrepancy: `REPAIR_REPORT.md` says the plan no longer contains any real-history deletion/reset language, which is factually incorrect for the reviewed SHA.
- Required correction: replace the T1.4 rollback with the same protected-state rule already used in B.5/T2.6. Only a path explicitly created as a disposable test database may be rebuilt. Update the repair report to acknowledge the missed line rather than claiming the first repair fully closed it.
- Acceptance test: a repository search for reset/delete/rebuild language finds no instruction treating real `var/` history or recovery evidence as disposable.

### [P1] G0-006 — Port dependency direction and Adapter ownership remain internally inconsistent

- Location: `governance/IMPLEMENTATION_PLAN.md`, B.9, T1.4/T1.6, T3.1 and T4.1–T4.3
- Evidence:
  1. B.9 still draws `Core → Ports ← Adapters`, while the corrected T2.4 says the Orchestrator reads through `HistoryPort` and passes immutable values into Core. For this design, the dependency is `Orchestrator → Core` and `Orchestrator → Ports ← Adapters`; pure Core should not depend on LLM, Delivery, History or data-source Ports.
  2. T1.4 implements the concrete SQLite runtime layer before T1.6 defines `HistoryPort`; T1.4 has no dependency on T1.6. This reverses the stated “contract before implementation” direction.
  3. T3.1 claims concrete implementations for LLM Provider, Markdown and PushPlus, while G4/T4.1–T4.3 separately assigns those same implementations. This creates duplicate Gate ownership and ambiguous acceptance responsibility.
- Impact: G1 may design SQLite around its own abstraction and retrofit `HistoryPort`; G3 and G4 may both implement the same adapters; architecture tests will not know whether Core is allowed to call Ports.
- Required correction:
  - Change the dependency diagram/text so Orchestrator depends on both pure Core and Port contracts; Adapters implement Ports; Core consumes/returns domain values and does not call external Ports.
  - Reorder or renumber G1 so minimal Port contracts exist before concrete SQLite implementation, and make the SQLite task explicitly implement the relevant Port contract.
  - Restrict G3 concrete implementation scope to data/enrichment/browser/critic adapters. Keep LLM generation, Markdown output and PushPlus Delivery implementations solely in G4. Shared adapter conventions may be established earlier, but implementation ownership must be unique.
- Acceptance test: the task dependency graph places each concrete implementation after its Port; every Adapter has exactly one implementation Gate; Core has no dependency arrow to external Ports.

### Re-review verdict

**CHANGES_REQUESTED**

The repair substantially improves the plan and correctly closes G0-001, G0-003, G0-004 and G0-005. G0-002 remains open and G0-006 is a new blocking consistency finding. The next repair should be a narrow governance-only commit; it must not enter G1 or modify production code.

---

## Re-review 2 — candidate `4ba0d34ceb474513292edf4217d8ba58814a303c`

### Verification

- Original base: `8a2e82072afca8fe7217c9fc79288d99a94e01ce`
- Repair candidate: `4ba0d34ceb474513292edf4217d8ba58814a303c`
- Previous review commit: `63f31fb5e47b3569ee4cdeb3d1e3efea42a50087`
- Branch observed: `exec/g0-plan`
- Worktree at review start: clean
- `git diff --check 63f31fb..4ba0d34`: pass
- Full lineage: base → original candidate → review 1 → repair 1 → review 2 → repair 2

The Reviewer inspected the complete second repair delta, searched the revised plan for unsafe deletion/reset language and stale dependency references, checked the new G1 ordering, and regressed the full original-base-to-new-candidate governance range.

### Finding closure

| Finding | Final status | Evidence |
|---|---|---|
| G0-001 (P1) Port contracts after Orchestrator | CLOSED | Port contracts are G1/T1.4; G2 uses fake/in-memory implementations |
| G0-002 (P1) Core/storage coupling and unsafe rollback | CLOSED | Core consumes immutable history inputs; real runtime state is protected in B.5, T1.5 and T2.6 |
| G0-003 (P2) WorkBuddy memory policy | CLOSED | `.workbuddy/` is intentionally ignored and worktree is clean |
| G0-004 (P2) Risk inconsistency | CLOSED | Priorities and references align with `RISK_REGISTER.md` |
| G0-005 (P2) Owner/ADR overload | CLOSED | Decisions are classified by actual authority and reversibility |
| G0-006 (P1) Dependency direction and Adapter ownership | CLOSED | Orchestrator depends on Core and Ports; Adapters implement Ports; G1 is contract-before-SQLite; G3 owns data adapters and G4 owns LLM/output/delivery |

### [P2] G0-007 — One stale task reference remains after G1 renumbering (non-blocking)

- Location: `governance/IMPLEMENTATION_PLAN.md`, T2.6 Tests (approximately line 285)
- Evidence: the sentence says the task runs against Port contracts defined by `T1.6`; after the accepted renumbering, Ports are T1.4 and T1.6 is test infrastructure. The T2.6 Dependencies field correctly points to T1.4, and all architectural descriptions also point to T1.4.
- Impact: documentation typo only; it does not reopen the Port ordering or change implementation ownership.
- Required follow-up: when G1 begins and synchronizes task documentation, change that T2.6 reference from T1.6 to T1.4. Also make explicit in the G1 Port definition whether run-journal operations are part of `HistoryPort` or a separately named `RunJournalPort`; either is acceptable if the boundary is minimal and contract-tested.
- Blocking status: non-blocking P2; must be disclosed in the G1 plan/report and verified at G1 review.

### Final G0 verdict

**ACCEPTED**

Acceptance applies only to candidate `4ba0d34ceb474513292edf4217d8ba58814a303c` against base `8a2e82072afca8fe7217c9fc79288d99a94e01ce`. All P0/P1 findings are closed. G0 may be merged into `main`; G1 may begin only after the merge is complete and a new G1 branch is created from the accepted merged baseline. G0-007 remains a disclosed P2 follow-up for G1.
